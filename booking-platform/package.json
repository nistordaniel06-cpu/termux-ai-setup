{
  "name": "booking-platform",
  "private": true,
  "workspaces": [
    "packages/api",
    "packages/web"
  ],
  "scripts": {
    "api:dev": "npm run dev --workspace packages/api",
    "api:test": "npm run test --workspace packages/api",
    "api:build": "npm run build --workspace packages/api",
    "web:dev": "npm run dev --workspace packages/web",
    "web:build": "npm run build --workspace packages/web"
  }
}
